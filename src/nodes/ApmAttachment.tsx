import React, { FunctionComponent } from 'react';
import { Text } from 'react-native';

const ApmAttachment: FunctionComponent = () => {
  return <Text>ApmAttachment</Text>;
};

export default ApmAttachment;
