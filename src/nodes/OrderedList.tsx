import React, { FunctionComponent } from 'react';
import { Text } from 'react-native';

const OrderedList: FunctionComponent = () => {
  return <Text>OrderedList</Text>;
};

export default OrderedList;
