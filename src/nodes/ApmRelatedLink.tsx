import React, { FunctionComponent } from 'react';
import { Text } from 'react-native';

const ApmRelatedLink: FunctionComponent = () => {
  return <Text>ApmRelatedLink</Text>;
};

export default ApmRelatedLink;
