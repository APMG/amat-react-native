export declare enum BodyType {
    Web = 0,
    AMP = 1,
    Native = 2,
    Minimal = 3
}
export interface EmbeddedAssets {
    audio: object;
    attachments: object;
    images: object;
    oembeds: object;
}
export interface InnerProps {
    key: any;
}
